<h2>Restaurants...</h2>
<ul>
<% @restaurants.each do |restaurant| %>
<li><%= restaurant.name %><%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary mb-4 ml-4"%></li>
<% end %>
</ul>

<%= link_to "Add a new restaurant", new_restaurant_path %>


